{
  "name": "list-api",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "src/main.ts",
  "private": true,
  "scripts": {
    "build:dev": "npm run build && NODE_ENV=development npm run synth",
    "build": "rm -rf dist && tsc",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok",
    "upgrade": "npm i cdktf@latest cdktf-cli@latest",
    "upgrade:next": "npm i cdktf@next cdktf-cli@next"
  },
  "engines": {
    "node": ">=12"
  },
  "dependencies": {
    "@pocket-tools/terraform-modules": "1.26.0",
    "cdktf": "^0.5.0",
    "constructs": "^3.3.245"
  },
  "devDependencies": {
    "@types/node": "16.11.26",
    "cdktf-cli": "^0.8.6",
    "typescript": "4.6.2"
  }
}
